# 🌍 Многоязычная поддержка Zen Finance

## ✅ Что было реализовано

### 1. **Централизованная система переводов** 
   - Файл: [src/lib/translations.ts](src/lib/translations.ts)
   - Полная локализация на **русский** и **английский** языки
   - Структурированная система с простым расширением для новых языков

### 2. **Автоматическое определение языка браузера**
   - Функция `detectBrowserLanguage()` в `src/lib/translations.ts`
   - Логика определения:
     - 🇷🇺 **Русский** → RU, UK (украинский), BY (белорусский)
     - 🇬🇧 **Английский** → все остальные языки
   - Автоматическая инициализация при первой загрузке приложения

### 3. **Локализованные компоненты**

#### SettingsModal.tsx
- Полностью переведены все элементы меню настроек
- Выбор темы (Light/Dark) на двух языках
- Выбор языка (RU/EN) с эмодзи флагов
- Информационный текст на выбранном языке

#### page.tsx (главная страница)
- Интегрирована централизованная система переводов
- Добавлена инициализация языка браузера при загрузке
- Все текстовые строки используют переводы из `translations.ts`

#### Filters.tsx
- Переведены все фильтры (Все, День, Неделя, Месяц)
- Переведены типы операций (Расходы/Expenses, Доходы/Incomes)

#### AddTransaction.tsx
- Переведены все элементы модального окна добавления транзакции
- Переведены кнопки и сообщения об ошибках
- Переведены названия валют (RUB, USD)

### 4. **Хранилище состояния (Zustand)**
- [src/store/useStore.tsx](src/store/useStore.tsx) обновлено
- Добавлена поддержка `detectBrowserLanguage()`
- Сохранение выбранного языка в `localStorage`

## 📦 Структура переводов

```typescript
export const translations = {
  ru: {
    // Все текстовые строки на русском
  },
  en: {
    // Все текстовые строки на английском
  }
}
```

### Доступные ключи перевода:
- **Основное**: `greet`, `balance`, `income`, `expense`, `history`
- **Модальные окна**: `settingsTitle`, `themeLabel`, `languageLabel`
- **Фильтры**: `all`, `day`, `week`, `month`, `expenses`, `incomes`
- **Действия**: `delete`, `save`, `add`, `edit`, `create`
- **Валюты**: `RUB`, `USD`

## 🚀 Как это работает

### При первой загрузке:
1. Приложение определяет язык браузера пользователя
2. Если браузер на RU/UK/BE → устанавливается русский язык
3. Для всех остальных языков → английский
4. Выбор сохраняется в `localStorage` (компонент `zen-finance-storage`)

### При переключении языка:
1. Пользователь нажимает на кнопку выбора языка в Settings
2. Выбор сохраняется в `zustand` хранилище
3. Все компоненты автоматически обновляются через реактивность React
4. Выбор синхронизируется с `localStorage`

## 📝 Файлы, которые были изменены

| Файл | Изменения |
|------|-----------|
| [src/lib/translations.ts](src/lib/translations.ts) | ✨ **Создан** - Центральное хранилище переводов |
| [src/store/useStore.tsx](src/store/useStore.tsx) | 🔄 Импорт `detectBrowserLanguage` |
| [src/app/page.tsx](src/app/page.tsx) | 🔄 Интеграция централизованных переводов + инициализация браузера |
| [src/components/SettingsModal.tsx](src/components/SettingsModal.tsx) | 🔄 Полная локализация UI |
| [src/components/Filters.tsx](src/components/Filters.tsx) | 🔄 Использование централизованных переводов |
| [src/components/AddTransaction.tsx](src/components/AddTransaction.tsx) | 🔄 Использование централизованных переводов |

## 💡 Примеры использования

### В компоненте:
```tsx
import { translations } from '@/lib/translations';
import { useFinanceStore } from '@/store/useStore';

export default function MyComponent() {
  const { lang } = useFinanceStore();
  const t = translations[lang as keyof typeof translations];
  
  return <h1>{t.settingsTitle}</h1>;
}
```

### Определение языка браузера:
```tsx
import { detectBrowserLanguage } from '@/lib/translations';

const browserLang = detectBrowserLanguage();
// Вернет 'ru' или 'en'
```

## 🔧 Добавление нового языка

1. Добавьте язык в `translations.ts`:
```typescript
export const translations = {
  ru: { /* ... */ },
  en: { /* ... */ },
  fr: { /* французский */ }  // ← Новый язык
}
```

2. Обновите тип:
```typescript
export type Language = 'ru' | 'en' | 'fr';
```

3. Добавьте кнопку выбора в `SettingsModal.tsx`

## ✨ Особенности реализации

✅ **Type-safe** - используется TypeScript для контроля доступа к переводам  
✅ **Автоматическое определение** - не требует явной конфигурации  
✅ **Сохранение выбора** - использует `localStorage` для персистентности  
✅ **Реактивность** - все компоненты обновляются при смене языка  
✅ **Расширяемость** - легко добавить новые языки  
✅ **Production-ready** - прошел `npm run build` без ошибок  

---

**Проект Zen Finance теперь полностью локализирован на русский и английский языки! 🎉**
